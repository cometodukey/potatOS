fix the double faults
fix the register dump values in panic

interrupts - exceptions
GDT for userland
paging and vmm
kprintf padding

panic, assert, __stack_chk_fail safe functions. could be achieved through atomic rd/wr

lockless pmm and other shared memory? use atomic vars?
initramfs
allow config macros to be set at compile time
fix console last row bug
clang-format
use an LDT?
